# Clerk Authentication
# Get this from: https://dashboard.clerk.com → Your App → API Keys
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Background writes API (Recommended)
# If set, the CMS will post changes to this endpoint (Cloudflare Worker)
VITE_WRITE_API_BASE=https://your-worker-subdomain.workers.dev

# Optional fallback (Client-side writes)
# Only if you are NOT using VITE_WRITE_API_BASE
# Generate at: https://github.com/settings/personal-access-tokens (fine-grained)
# Scope: Contents (Read/Write) to the single repo Maxnate-Africa/swai-electronics-testing
# VITE_GITHUB_TOKEN=ghp_your_token_here
